[package]
name = "andreadimatteo"
version = "0.1.0"
edition = "2024"

[dependencies]
regex = "1"
serde = { version = "1", features = ["derive"] }
serde_yaml = "0.9"
chrono = { version = "0.4", features = ["serde"] }
fs_extra = "1.3"
walkdir = "2.5.0"
